node_modules/
dist/
.env
*.log
data/*.db
data/*.db-*
# Accidental SQLite file when DATABASE_URL was resolved incorrectly (legacy)
:memory:

# Agent / planning artifacts (no commitear)
.agent-handoff.md
**/.DS_Store
