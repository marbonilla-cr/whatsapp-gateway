/** Default tenant for new apps created via admin when `tenantId` is omitted (Antigua Lechería). */
export const DEFAULT_CLIENT_TENANT_ID = 'tenant_antigua_lecheria';

export const TENANT_MBCSOFT_ID = 'tenant_mbcsoft';
