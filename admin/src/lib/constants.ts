/** Must match `DEFAULT_CLIENT_TENANT_ID` in gateway `src/db/constants.ts`. */
export const DEFAULT_CLIENT_TENANT_ID = 'tenant_antigua_lecheria';

/** MBCSOFT internal tenant — matches `TENANT_MBCSOFT_ID` in gateway. */
export const TENANT_MBCSOFT_ID = 'tenant_mbcsoft';
